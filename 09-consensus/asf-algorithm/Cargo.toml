[package]
name = "asf-algorithm"
version = "0.1.0"
edition = "2021"
authors = ["Ëtrid Foundation"]
description = "FODDoS ASF (Ascending Scale of Finality) core consensus algorithm"
license = "Apache-2.0"

[dependencies]
# Substrate primitives
sp-core = { version = "31.0.0", default-features = false }
sp-std = { version = "14.0.0", default-features = false }
sp-runtime = { version = "37.0.0", default-features = false }
sp-io = { version = "35.0.0", default-features = false }

# Codec
codec = { package = "parity-scale-codec", version = "3.6.12", default-features = false, features = ["derive"] }
scale-info = { version = "2.11.3", default-features = false, features = ["derive"] }

# Serialization
serde = { version = "1.0.197", default-features = false, features = ["derive", "alloc"] }

# Logging
log = { version = "0.4.21", default-features = false }

# Thiserror for error handling
thiserror = { version = "1.0", optional = true }

[dev-dependencies]
sp-io = { version = "35.0.0" }
sp-core = { version = "31.0.0" }

[features]
default = ["std"]
std = [
    "sp-core/std",
    "sp-std/std",
    "sp-runtime/std",
    "sp-io/std",
    "codec/std",
    "scale-info/std",
    "serde/std",
    "log/std",
    "thiserror",
]
