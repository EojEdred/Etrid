# EDSC Relayer Service Configuration

# ======================================
# Attestation Services
# ======================================

# Comma-separated list of attestation service URLs
ATTESTATION_SERVICE_URLS=http://localhost:3000,http://localhost:3001

# ======================================
# Chain Connections
# ======================================

# Ëtrid Substrate WebSocket URL
SUBSTRATE_WS_URL=ws://localhost:9944

# Ethereum RPC URL (HTTP or WebSocket)
ETHEREUM_RPC_URL=http://localhost:8545

# ======================================
# Relayer Identity
# ======================================

# Your relayer's Ethereum/Substrate address
RELAYER_ADDRESS=0x1234567890123456789012345678901234567890

# Your relayer's private key (secp256k1 for Ethereum, SR25519 for Substrate)
# WARNING: Keep this secret! Use environment-specific secure key management in production
RELAYER_PRIVATE_KEY=0x0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ======================================
# Contract Addresses
# ======================================

# Ethereum MessageTransmitter contract address
MESSAGE_TRANSMITTER_ADDRESS=0x0000000000000000000000000000000000000000

# Ethereum TokenMessenger contract address (optional)
TOKEN_MESSENGER_ADDRESS=0x0000000000000000000000000000000000000000

# ======================================
# Polling Settings
# ======================================

# Poll interval in milliseconds (default: 30 seconds)
POLL_INTERVAL_MS=30000

# Maximum retry attempts for failed relays
MAX_RETRIES=3

# Delay between retries in milliseconds (default: 1 minute)
RETRY_DELAY_MS=60000

# ======================================
# Ethereum Gas Settings
# ======================================

# Gas limit for relay transactions (optional, uses estimate if not set)
GAS_LIMIT=500000

# Max fee per gas in gwei (optional, uses network default if not set)
MAX_FEE_PER_GAS=50

# Max priority fee per gas in gwei (optional)
MAX_PRIORITY_FEE_PER_GAS=2

# ======================================
# API Settings (Optional)
# ======================================

# Enable HTTP API for status monitoring
ENABLE_API=false

# API server port
API_PORT=3001

# Logging level (error, warn, info, debug)
LOG_LEVEL=info
