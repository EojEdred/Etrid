{
  "name": "@etrid/bridge-tests",
  "version": "1.0.0",
  "description": "Integration and E2E tests for EDSC cross-chain bridge",
  "scripts": {
    "test": "jest",
    "test:integration": "jest integration",
    "test:e2e": "jest e2e",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "setup:ethereum": "cd ../contracts/ethereum && npx hardhat node",
    "setup:substrate": "cd .. && ./scripts/start-local-nodes.sh",
    "deploy:ethereum": "cd ../contracts/ethereum && npx hardhat run scripts/deploy.js --network localhost",
    "deploy:substrate": "echo 'Substrate pallets built into runtime'",
    "start:attestation": "cd ../services/attestation-service && npm run dev",
    "start:relayer": "cd ../services/relayer-service && npm run dev"
  },
  "keywords": [
    "edsc",
    "bridge",
    "testing",
    "integration"
  ],
  "author": "Ëtrid Team",
  "license": "Apache-2.0",
  "dependencies": {
    "@polkadot/api": "^10.11.1",
    "@polkadot/keyring": "^12.6.2",
    "@polkadot/util": "^12.6.2",
    "@polkadot/util-crypto": "^12.6.2",
    "ethers": "^6.9.0",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
