# ËTRID SEO and Redirect Configuration

# Enable Rewrite Engine
RewriteEngine On

# Redirect broken subdomain paths to appropriate locations
RewriteRule ^wallet/?$ / [R=301,L]
RewriteRule ^explorer/?$ /telemetry [R=301,L]
RewriteRule ^bridge/?$ / [R=301,L]
RewriteRule ^masterchef/?$ / [R=301,L]
RewriteRule ^validator-dashboard/?$ /validators/ [R=301,L]
RewriteRule ^faucet/?$ / [R=301,L]
RewriteRule ^blog/?$ / [R=301,L]
RewriteRule ^forum/?$ / [R=301,L]
RewriteRule ^governance/?$ / [R=301,L]

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Remove www prefix
RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
RewriteRule ^(.*)$ https://%1/$1 [R=301,L]

# Custom error pages
ErrorDocument 404 /index.html

# Security Headers
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
</IfModule>

# Cache Control
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/html "access plus 1 hour"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
</IfModule>
