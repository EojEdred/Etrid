[package]
name = "etrid-integration-tests"
version = "0.1.0"
edition = "2021"
authors = ["Ëtrid Foundation"]
description = "Integration tests for Ëtrid custom block structures"

[dependencies]
etrid-primitives = { path = "../../../05-multichain/primitives" }
sp-core = { workspace = true }
sp-runtime = { workspace = true }

[dev-dependencies]
# No additional dev dependencies needed for basic tests
