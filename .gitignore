
target/
node_modules/
*.rlib
*.rmeta
*.wasm
*.log
*.tmp
.DS_Store

# Multi-node testing artifacts (temporary)
.local-testnet/
.peering-test/
.validator-test/
.test-network/
.bridge-test/
*.log
collator_test_results.txt
.peering-test-output.log

# Build logs from PBC compilation
.*-build.log
.*.log

# Backup files
*.bak
*.bak2
*~

# Frontend build artifacts
.next/
dist/
build/
out/
.cache/
.vite/
.turbo/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo

# Chain spec generated files
*.raw.json
flarechain-shared-raw.json

# Build and test artifacts
build*.log
test_results.log
*.profraw
coverage/

# Environment files (keep examples)
.env
!.env.example
!.env.*.example

# ============================================================================
# VALIDATOR KEYS AND SENSITIVE DATA - DO NOT COMMIT!
# ============================================================================

# Consolidated secrets folder - NEVER COMMIT!
secrets/
**/secrets/

# Generated validator keys (contains private keys, sudo keys, payment accounts)
validator-keys-setup/generated-keys/
**/generated-keys*/
**/generated-keys-*/

# Any JSON files with "keys" in the name (likely contain private keys)
*-keys-complete.json
*-keys-*.json
validator-keys*.json
sudo-key*.json

# Encrypted key files (even encrypted, don't commit to public repos)
*.enc
*.gpg
*.asc
*.key
*.pem
*.p12
*.pfx

# Backup files that might contain keys
*-backup*.txt
*-backup*.json
ssh-keys-backup*
keys-backup*

# Validator inventory files (may contain IP addresses and sensitive info)
validator-inventory.txt
*-inventory.txt

# Chain spec with embedded keys
*-chainspec-*.json
genesis-*.json

# API tokens and credentials
*-token.txt
*-credentials.json
*-secrets.json

# Runtime data directories (generated by services, not part of source)
14-aidevs/data/
**/data/vectordb/
**/data/grafana/
**/data/prometheus/
**/data/redis/
**/data/postgres/

# Archive folders (old session reports, temporary docs)
archive-docs/
archive/
_archive*/
_CACHE_ARCHIVE/

# Temporary genesis account generation
genesis-accounts-*/

# BFG cleanup reports
..bfg-report/

# Compiled binaries (should be built from source)
**/flarechain-node
**/flare-chain-node
*.tar.gz
!release-packages/**/*.tar.gz
# But exclude large tarballs from release-packages
release-packages/**/*Darwin*.tar.gz
release-packages/**/flarechain-node

# Deployment packages (binaries only, keep config)
mainnet-deployment-package/*.node
mainnet-deployment-package/flarechain-node
validator-deployment-kit/*.node

# ============================================================================
# AI MONITORING SYSTEM - SENSITIVE DATA
# ============================================================================

# AI API keys and environment files
ai-monitoring/.env
ai-monitoring/.env.clean
ai-monitoring/.env.production
ai-monitoring/.env.local

# AI monitoring runtime data
ai-monitoring/logs/*.log
ai-monitoring/GLOBAL_MEMORY.md
etrid-monitoring-package.tar.gz

# Python cache
ai-monitoring/__pycache__/
ai-monitoring/**/__pycache__/
*.pyc
*.pyo
