# Ëtrid Governance Forum Environment Configuration
# Copy this file to .env.forum and fill in your values

# =============================================================================
# DISCOURSE CORE SETTINGS
# =============================================================================

# Domain where your forum will be hosted
DISCOURSE_HOSTNAME=forum.etrid.network

# Admin email (will receive notifications and be the first admin account)
ADMIN_EMAIL=admin@etrid.network

# =============================================================================
# DATABASE
# =============================================================================

# PostgreSQL password (CHANGE THIS IN PRODUCTION!)
POSTGRES_PASSWORD=your_secure_database_password_here

# =============================================================================
# EMAIL CONFIGURATION (SMTP)
# =============================================================================

# Email provider settings (example uses SendGrid, but any SMTP works)
# Free SendGrid account: 100 emails/day
# Alternatives: Mailgun, AWS SES, Postmark, etc.

SMTP_ADDRESS=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=your_sendgrid_api_key_here

# For Gmail SMTP (not recommended for production):
# SMTP_ADDRESS=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-specific-password

# =============================================================================
# PORTS
# =============================================================================

# Port where forum will be accessible (default: 8080)
FORUM_PORT=8080

# SSL ports (only used with nginx profile)
HTTP_PORT=80
HTTPS_PORT=443

# Mail receiver port (only used with mail profile)
MAIL_PORT=25

# =============================================================================
# SSL/HTTPS (Optional - for production)
# =============================================================================

# Set to true to force HTTPS redirects
FORCE_HTTPS=false

# =============================================================================
# CDN & OBJECT STORAGE (Optional)
# =============================================================================

# AWS S3 for uploads and assets (recommended for production)
USE_S3=false
S3_BUCKET=
S3_REGION=us-east-1
S3_ACCESS_KEY=
S3_SECRET_KEY=

# CDN URL (e.g., CloudFlare, AWS CloudFront)
CDN_URL=

# =============================================================================
# BLOCKCHAIN INTEGRATION
# =============================================================================

# Ëtrid RPC endpoint for on-chain verification
ETRID_RPC_URL=http://localhost:9933

# Treasury pallet account (for proposal verification)
TREASURY_ACCOUNT=

# Foundation multisig address (for official announcements)
FOUNDATION_MULTISIG=

# =============================================================================
# MAIL RECEIVER (Optional)
# =============================================================================

# Enable email-based posting (advanced feature)
MAIL_RECEIVER_HOST=mail.etrid.network
MAIL_RECEIVER_PORT=25

# =============================================================================
# BACKUP CONFIGURATION (Optional)
# =============================================================================

# Automatic backup destination
BACKUP_LOCATION=/backups
BACKUP_FREQUENCY=daily

# S3 backup destination
BACKUP_S3_BUCKET=
BACKUP_S3_REGION=
BACKUP_S3_ACCESS_KEY=
BACKUP_S3_SECRET_KEY=
