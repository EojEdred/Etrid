name: CI – Transaction Engine

on:
  push:
    paths:
      - './Etrid_Phase1_Missing_Repos/tx-engine/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./Etrid_Phase1_Missing_Repos/tx-engine

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build and Test
        run: |
          cargo build --verbose
          cargo test --verbose
