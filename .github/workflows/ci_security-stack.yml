name: CI – Security Stack

on:
  push:
    paths:
      - './Etrid_Phase1_Missing_Repos/security-stack/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./Etrid_Phase1_Missing_Repos/security-stack

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build and Test
        run: |
          cargo build --verbose
          cargo test --verbose
