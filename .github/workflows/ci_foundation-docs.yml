name: CI – Foundation Docs

on:
  push:
    paths:
      - './Etrid_Phase1_Missing_Repos/foundation-docs/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./Etrid_Phase1_Missing_Repos/foundation-docs

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build and Test
        run: |
          cargo build --verbose
          cargo test --verbose
